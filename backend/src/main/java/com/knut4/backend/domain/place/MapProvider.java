package com.knut4.backend.domain.place;

public interface MapProvider {
    // Strategy interface for place search (Naver, Kakao)
    // List<PlaceDto> search(String query, double lat, double lng);
}
